{
  "name": "notebridge",
  "version": "0.1.0",
  "description": "Multi-platform note bridge — translate notes between WeRead, GetNote, Obsidian, Flomo, and more",
  "main": "dist/index.js",
  "bin": {
    "notebridge": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "note",
    "bridge",
    "weread",
    "getnote",
    "obsidian",
    "flomo",
    "markdown"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
